#include <iostream>
#include "./../include/Multicast.hpp"

int main(int argc, char const *argv[])
{
  // Test one, sender
  // char msg[] = "Mensagem de texto";
  // char ip[] = "224.0.0.1";
  // Multicast m(ip, 65534);
  // m.sendMessage(msg);
  
  // Test two, receiver
  // char ip[] = "224.0.0.1";
  // Multicast m(ip, 65534);
  
  // while(1){
  //   std::cout << m.receiveMessage << std::endl;
  // }
  return 0;
}
